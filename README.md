# Religion-rag-app


## Installation Hints : 

First you must install "Requirements.txt" Because of It includes all Packages 
That The project Won't able to run without it . 

To install it You should write this command : 


```bash
pip install -r requirements.txt

```
